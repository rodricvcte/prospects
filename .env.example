SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Gerar com: openssl rand -base64 32
AUTH_SECRET=
# Chave fixa usada pela extensão de Chrome (não faz login interativo).
# Gerar com: openssl rand -hex 32 — e replicar em extension/config.local.js
EXTENSION_API_KEY=
